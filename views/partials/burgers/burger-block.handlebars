<li>
    <p>{{this.id}}. {{this.burger_name}}</p>
    <button type="button" class="change-devour btn btn-danger btn-sm" data-id="{{id}}" data-newdevour="{{devoured}}">
        {{#unless devour}}DEVOUR ME!{{/unless}}
    </button>
</li>